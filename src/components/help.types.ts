/* Gemeinsame Formen der Hilfetexte. Eigene Datei, damit die deutsche und die
 * englische Fassung einander nicht einführen müssen. */
export type Kapitel = { titel: string; text: JSX.Element };
export type Tipp = { h: string; b: string };
